---
import Layout from "./Layout.astro";
const { frontmatter } = Astro.props;

---
<Layout>
 <h1>{frontmatter.title}</h1>
<p>escrito por: autor{frontmatter.author}</p>
<img src={frontmatter.image.url} width="300" alt={frontmatter.image.alt} />
<slot />
</Layout>